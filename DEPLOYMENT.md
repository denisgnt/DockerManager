# 🚀 Инструкция по развертыванию Docker Manager

## ✅ Что уже сделано

### Созданные файлы
- ✅ `package.json` - конфигурация проекта с зависимостями
- ✅ `vite.config.js` - настройки Vite (frontend сборщик)
- ✅ `server/index.js` - Express backend + Socket.IO
- ✅ `src/App.jsx` - главный компонент React
- ✅ `src/main.jsx` - точка входа с темой Material UI
- ✅ `src/components/ContainerList.jsx` - список контейнеров
- ✅ `src/components/ContainerLogs.jsx` - просмотр логов в реальном времени
- ✅ Документация (README.md, QUICK_START.md, и др.)

### Установленные зависимости
- ✅ React 18.2 + React DOM
- ✅ Material UI 5.14 (компоненты + иконки)
- ✅ Express 4.18 (backend)
- ✅ Socket.IO 4.7 (WebSocket)
- ✅ Axios (HTTP клиент)
- ✅ Vite 5.0 (сборщик)
- ✅ Concurrently (запуск frontend + backend)

### Текущий статус
🟢 **Приложение запущено и работает!**

- Frontend: http://localhost:3001
- Backend: http://localhost:5001
- Docker API: http://10.174.18.242:2375

## 📋 Что работает сейчас

✅ Отображение списка Docker контейнеров  
✅ Автообновление списка каждые 5 секунд  
✅ Запуск/Остановка/Перезапуск контейнеров  
✅ Удаление контейнеров  
✅ Просмотр логов в реальном времени через WebSocket  
✅ Адаптивный дизайн (desktop/tablet/mobile)  
✅ Темная тема Material UI  
✅ Обработка ошибок  

## 🎯 Следующие шаги

### 1. Тестирование приложения

Откройте в браузере: **http://localhost:3001**

Проверьте следующие функции:
- [ ] Отображаются ли контейнеры Docker
- [ ] Можно ли запустить/остановить контейнер
- [ ] Работает ли просмотр логов в реальном времени
- [ ] Обновляется ли список автоматически
- [ ] Правильно ли отображаются статусы (цветные индикаторы)

### 2. Если нужно изменить Docker API

Отредактируйте файл `server/index.js`:

```javascript
const DOCKER_HOST = 'http://YOUR_DOCKER_HOST:2375';
```

Затем перезапустите сервер.

### 3. Запуск в production

#### Вариант A: Production сборка frontend + backend

```bash
# 1. Создать production сборку frontend
npm run build

# 2. Добавить в server/index.js раздачу статики:
app.use(express.static('dist'));

# 3. Запустить только backend
npm run server
```

Теперь приложение доступно на http://localhost:5001

#### Вариант B: Использование PM2 (рекомендуется)

```bash
# Установить PM2
npm install -g pm2

# Создать production сборку
npm run build

# Запустить с PM2
pm2 start server/index.js --name docker-manager

# Автозапуск при перезагрузке
pm2 startup
pm2 save
```

#### Вариант C: Docker контейнер (мета-подход)

Создайте `Dockerfile`:

```dockerfile
FROM node:18-alpine

WORKDIR /app

COPY package*.json ./
RUN npm ci --only=production

COPY . .
RUN npm run build

EXPOSE 5001

CMD ["node", "server/index.js"]
```

Запуск:

```bash
docker build -t docker-manager .
docker run -p 5001:5001 docker-manager
```

### 4. Настройка NGINX (опционально)

Для production рекомендуется использовать NGINX как reverse proxy:

```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://localhost:5001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    location /socket.io/ {
        proxy_pass http://localhost:5001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}
```

### 5. Настройка переменных окружения

Создайте файл `.env` (скопируйте из `.env.example`):

```bash
cp .env.example .env
```

Отредактируйте значения:

```env
DOCKER_HOST=http://10.174.18.242:2375
PORT=5001
```

Обновите `server/index.js` для чтения из .env:

```javascript
import dotenv from 'dotenv';
dotenv.config();

const DOCKER_HOST = process.env.DOCKER_HOST || 'http://10.174.18.242:2375';
const PORT = process.env.PORT || 5001;
```

### 6. Безопасность для production

⚠️ **КРИТИЧЕСКИ ВАЖНО** для production:

```bash
# 1. Настройте TLS для Docker API (порт 2376)
# См. docker-api-setup.md

# 2. Добавьте авторизацию в приложение
# - JWT токены
# - OAuth2
# - Basic Auth

# 3. Настройте HTTPS для веб-интерфейса
# - Let's Encrypt сертификаты
# - NGINX с SSL

# 4. Ограничьте доступ файрволом
sudo ufw allow from YOUR_IP to any port 5001

# 5. Используйте переменные окружения для чувствительных данных
```

## 🔧 Полезные команды

### Разработка

```bash
# Запуск dev режима (frontend + backend)
npm run dev

# Запуск только frontend
npm run client

# Запуск только backend
npm run server

# Проверка подключения к Docker API
curl http://10.174.18.242:2375/version
```

### Production

```bash
# Создание сборки
npm run build

# Просмотр production сборки
npm run preview

# Установка только production зависимостей
npm ci --only=production
```

### PM2 управление

```bash
# Список процессов
pm2 list

# Логи
pm2 logs docker-manager

# Перезапуск
pm2 restart docker-manager

# Остановка
pm2 stop docker-manager

# Удаление
pm2 delete docker-manager
```

## 📁 Важные файлы для настройки

| Файл | Назначение | Что настраивать |
|------|------------|-----------------|
| `server/index.js` | Backend сервер | Docker API URL, PORT |
| `vite.config.js` | Frontend config | Dev server port, proxy |
| `package.json` | Зависимости | Scripts, versions |
| `.env` | Окружение | Секретные переменные |

## 🐛 Решение проблем

### Проблема: Не подключается к Docker API

```bash
# Проверьте доступность API
curl http://10.174.18.242:2375/version

# Проверьте на Docker хосте
sudo netstat -tlnp | grep 2375

# Проверьте файрвол
sudo ufw status
```

### Проблема: WebSocket не работает

1. Проверьте консоль браузера (F12) на ошибки
2. Убедитесь, что backend запущен
3. Проверьте настройки прокси в `vite.config.js`

### Проблема: Порты заняты

```bash
# Найти процесс на порту
lsof -i :3001
lsof -i :5001

# Убить процесс
kill -9 PID
```

## 📚 Документация проекта

- **README.md** - полная документация
- **QUICK_START.md** - быстрый старт
- **PROJECT_STRUCTURE.md** - структура проекта
- **UI_DESIGN.md** - описание интерфейса
- **docker-api-setup.md** - настройка Docker API

## 🎉 Готово к использованию!

Ваше приложение **Docker Manager** полностью создано и запущено!

**Текущий адрес:** http://localhost:3001

Теперь вы можете:
1. ✅ Просматривать все Docker контейнеры
2. ✅ Управлять контейнерами (start/stop/restart/delete)
3. ✅ Смотреть логи в реальном времени
4. ✅ Получать автообновления статусов

---

**Дата развертывания:** 2 ноября 2025 г.  
**Версия:** 1.0.0  
**Статус:** ✅ Успешно развернуто и работает
