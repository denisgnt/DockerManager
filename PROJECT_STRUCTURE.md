# 📁 Структура проекта Docker Manager

## Созданные файлы

```
DockerManager/
│
├── 📄 package.json              # Конфигурация проекта и зависимости
├── 📄 package-lock.json         # Зафиксированные версии зависимостей
├── 📄 vite.config.js            # Конфигурация Vite (порты, прокси)
├── 📄 index.html                # HTML шаблон приложения
├── 📄 .gitignore                # Игнорируемые файлы Git
├── 📄 .env.example              # Пример конфигурации окружения
│
├── 📘 README.md                 # Основная документация проекта
├── 📘 QUICK_START.md            # Краткое руководство
├── 📘 docker-api-setup.md       # Инструкция по настройке Docker API
│
├── 📁 server/                   # Backend сервер
│   └── 📄 index.js              # Express сервер + Socket.IO + Docker API прокси
│
├── 📁 src/                      # Frontend приложение
│   ├── 📄 main.jsx              # Точка входа React (с темой Material UI)
│   ├── 📄 App.jsx               # Главный компонент приложения
│   │
│   └── 📁 components/           # React компоненты
│       ├── 📄 ContainerList.jsx # Компонент списка контейнеров (карточки)
│       └── 📄 ContainerLogs.jsx # Компонент просмотра логов в реальном времени
│
└── 📁 node_modules/             # Установленные зависимости
```

## 📦 Установленные пакеты

### Production Dependencies
- `react` + `react-dom` - библиотека для UI
- `@mui/material` + `@mui/icons-material` - Material UI компоненты
- `@emotion/react` + `@emotion/styled` - стилизация для MUI
- `express` - веб-фреймворк для Node.js
- `cors` - CORS middleware
- `axios` - HTTP клиент
- `socket.io` + `socket.io-client` - WebSocket библиотека

### Development Dependencies
- `vite` - сборщик и dev сервер
- `@vitejs/plugin-react` - плагин React для Vite
- `concurrently` - запуск нескольких команд одновременно
- `@types/react` + `@types/react-dom` - TypeScript типы

## 🔧 Конфигурационные файлы

### package.json
Определяет:
- Скрипты запуска (`dev`, `client`, `server`, `build`)
- Зависимости проекта
- Метаданные проекта

### vite.config.js
Настраивает:
- React плагин
- Dev сервер (порт 3000 → автоматически 3001 если занят)
- Прокси для API и WebSocket на backend (5001)

### server/index.js
Реализует:
- Express HTTP сервер
- Socket.IO WebSocket сервер
- REST API endpoints для управления Docker
- Стриминг логов в реальном времени
- Подключение к Docker API (10.174.18.242:2375)

## 🎨 React компоненты

### main.jsx
- Точка входа приложения
- Настройка темной темы Material UI
- Инициализация React

### App.jsx
- Главный компонент
- Управление состоянием приложения
- Fetch контейнеров с автообновлением
- Обработка действий с контейнерами
- AppBar с навигацией

### ContainerList.jsx
- Отображение сетки карточек контейнеров
- Статус с цветовыми индикаторами
- Кнопки управления (start/stop/restart/delete)
- Кнопка просмотра логов
- Адаптивная сетка (Grid)

### ContainerLogs.jsx
- Модальное окно для логов
- WebSocket подключение через Socket.IO
- Автоскролл к последним логам
- Индикатор подключения
- Стиль терминала (monospace)

## 🚀 Команды для работы

```bash
# Установка зависимостей
npm install

# Запуск в dev режиме (frontend + backend одновременно)
npm run dev

# Запуск только frontend
npm run client

# Запуск только backend
npm run server

# Production сборка
npm run build

# Предварительный просмотр production
npm run preview
```

## 🌐 Порты приложения

- **Frontend (Vite):** 3001 (автоматически выбран, т.к. 3000 занят)
- **Backend (Express):** 5001 (изменен с 5000, т.к. был занят)
- **Docker API:** 10.174.18.242:2375

## 📡 API Маршруты

### REST API
- `GET /api/containers` - список контейнеров
- `GET /api/containers/:id` - детали контейнера
- `POST /api/containers/:id/start` - запустить
- `POST /api/containers/:id/stop` - остановить
- `POST /api/containers/:id/restart` - перезапустить
- `DELETE /api/containers/:id` - удалить
- `GET /api/containers/:id/logs` - получить логи
- `GET /api/containers/:id/stats` - статистика
- `GET /api/info` - информация о Docker

### WebSocket Events
- `subscribe-logs` - подписка на логи контейнера
- `unsubscribe-logs` - отписка от логов
- `log-data` - получение данных логов
- `log-error` - ошибка при получении логов

## ✨ Особенности реализации

### Безопасность
- CORS настроен для localhost
- Backend выступает прокси для Docker API
- Клиент не имеет прямого доступа к Docker API

### Real-time обновления
- Автообновление списка контейнеров каждые 5 секунд
- WebSocket для потоковых логов
- Автоматическая реконнекция при обрыве соединения

### UX улучшения
- Автоскролл логов
- Цветовые индикаторы статусов
- Подтверждение удаления
- Уведомления об ошибках
- Адаптивный дизайн

### Performance
- React.StrictMode для обнаружения проблем
- Vite для быстрой горячей перезагрузки
- Оптимизированные пересборки

## 📚 Документация

- **README.md** - полная документация проекта
- **QUICK_START.md** - быстрое начало работы
- **docker-api-setup.md** - настройка Docker API
- **PROJECT_STRUCTURE.md** - этот файл

---

**Дата создания:** 2 ноября 2025 г.  
**Статус:** ✅ Готов к использованию
