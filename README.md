# Docker Manager

Современный веб-интерфейс для управления Docker контейнерами через Docker API.

## 🚀 Возможности

### Управление контейнерами
- ✅ Просмотр списка всех Docker контейнеров в **трех режимах**: список, плитка, граф зависимостей
- ✅ Управление контейнерами (старт, остановка, перезапуск, удаление)
- ✅ Подробная информация о контейнерах с полными метаданными
- ✅ Статистика использования ресурсов в реальном времени
- ✅ Сортировка, поиск и фильтрация контейнеров
- ✅ Автоматическое обновление списка (каждые 5 секунд)

### Логи и мониторинг
- ✅ Просмотр логов в реальном времени через WebSocket
- ✅ **Поддержка цветных логов (ANSI color codes)** - автоматическое отображение bash-цветов
- ✅ **Экспорт логов в файл** - выгрузка до 5000 последних строк
- ✅ Очистка ANSI-кодов при экспорте для удобного чтения
- ✅ Автопрокрутка логов с возможностью отключения

### Граф зависимостей
- ✅ **Визуализация зависимостей** между контейнерами на основе переменных окружения
- ✅ Автоматическое определение связей через URI и PORT переменные
- ✅ Интерактивный граф с возможностью перемещения узлов
- ✅ Сохранение позиций узлов между сеансами
- ✅ Поиск и фильтрация узлов на графе
- ✅ Цветовая индикация статуса контейнеров

### Выполнение скриптов
- ✅ **Автоматическое выполнение rebuild-скриптов** с сопоставлением по именам
- ✅ Потоковый вывод результатов выполнения в реальном времени
- ✅ Индикация процесса rebuilding для контейнеров
- ✅ Уведомления об успешном/неуспешном завершении

### Кеширование и производительность
- ✅ **In-memory кеш с персистентностью** (node-cache)
- ✅ Отображение недоступных контейнеров из кеша при недоступности Docker API
- ✅ Автоматическое обновление кеша каждые 5 минут
- ✅ Мердж стратегия для сохранения истории контейнеров

### Интерфейс
- ✅ Современный Material UI 7 дизайн
- ✅ Светлая и темная темы оформления
- ✅ Полностью адаптивный дизайн (desktop, tablet, mobile)
- ✅ Отображение версии приложения в AppBar
- ✅ Glassmorphism эффекты в темной теме

## 🛠 Технологии

### Frontend
- **React 19.2** - библиотека для создания пользовательских интерфейсов
- **React Compiler** - автоматическая оптимизация компонентов
- **React Router 7.9** - маршрутизация и управление URL
- **Zustand 5.0** - управление глобальным состоянием приложения
- **Vite 7.2** - современный сборщик проектов
- **Material UI 7.3** - библиотека компонентов
- **@xyflow/react 12.9** - библиотека для построения интерактивных графов
- **Recharts 3.5** - библиотека для визуализации статистики
- **Socket.IO Client 4.8** - для WebSocket соединений
- **Axios 1.13** - для HTTP запросов
- **ansi-to-react 6.1** - для отображения ANSI цветов в логах

### Backend
- **Node.js 22+** - серверная платформа
- **Express 5.1** - веб-фреймворк
- **Socket.IO 4.8** - для WebSocket сервера и real-time обновлений
- **Axios 1.13** - для взаимодействия с Docker API
- **node-cache 5.1** - in-memory кеширование с файловой персистентностью
- **node-cron** - планировщик задач для обновления кеша

## 📋 Требования

- Node.js 18+ и yarn
- **ИЛИ**
- Docker и Docker Compose

## 🚢 Запуск в Docker (рекомендуется)

Для production запуска используйте Docker Compose:

```bash
# Настройте .env файл
cp .env.docker .env
# Отредактируйте .env под ваши нужды

# Запустите приложение
docker-compose up -d
```

Приложение будет доступно на `http://localhost:3001`

📖 Подробнее см. [DOCKER_DEPLOYMENT.md](./DOCKER_DEPLOYMENT.md)

## 🔧 Локальная разработка

1. Клонируйте репозиторий или перейдите в директорию проекта:

```bash
cd DockerManager
```

2. Установите зависимости:

```bash
npm install
```

3. Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

4. Настройте переменные окружения в `.env`:

```env
VITE_DOCKER_API_HOST=10.174.18.242
VITE_DOCKER_API_PORT=2375
VITE_PORT=3001
VITE_BACKEND_PORT=5001
```

См. подробную документацию в [ENV_VARIABLES.md](./ENV_VARIABLES.md)

3. Если необходимо, измените адрес Docker API в файле `server/index.js`:

```javascript
const DOCKER_HOST = 'http://10.174.18.242:2375';
```

## 🚀 Запуск

### Режим разработки

Запустите приложение в режиме разработки (одновременно запускаются frontend и backend):

```bash
npm run dev
```

- Frontend будет доступен по адресу: `http://localhost:3000`
- Backend API будет доступен по адресу: `http://localhost:5000`

### Отдельный запуск компонентов

Запуск только backend сервера:

```bash
npm run server
```

Запуск только frontend (в другом терминале):

```bash
npm run client
```

### Продакшн сборка

Создание продакшн сборки:

```bash
npm run build
```

Предварительный просмотр продакшн сборки:

```bash
npm run preview
```

## 📁 Структура проекта

```
DockerManager/
├── server/
│   ├── index.js              # Express сервер с Socket.IO
│   ├── cacheManager.js       # Система кеширования (node-cache)
│   ├── appsettings.js        # Конфигурация сервера
│   └── utils.js              # Утилиты (определение зависимостей)
├── src/
│   ├── components/
│   │   ├── ContainerList.jsx      # Список/плитка контейнеров
│   │   ├── ContainerLogs.jsx      # Просмотр логов с ANSI
│   │   ├── ContainerInfo.jsx      # Детальная информация
│   │   ├── ContainerStats.jsx     # Графики статистики
│   │   ├── DependencyGraph.jsx    # Граф зависимостей
│   │   └── ScriptOutput.jsx       # Вывод выполнения скриптов
│   ├── store/
│   │   └── useDockerStore.js      # Zustand store
│   ├── App.jsx               # Главный компонент с роутингом
│   ├── main.jsx              # Точка входа React
│   ├── ThemeContext.jsx      # Контекст темы оформления
│   └── mobile.css            # Стили для мобильной версии
├── data/
│   └── cache/
│       ├── containers.json        # Кеш контейнеров
│       └── node-positions.json    # Позиции узлов графа
├── scripts/                  # Rebuild скрипты (опционально)
├── docker-compose.yml        # Docker Compose конфигурация
├── Dockerfile                # Production Docker образ
├── vite.config.js            # Конфигурация Vite
├── package.json              # Зависимости (v1.5.0)
└── README.md                 # Документация
```

## 🎯 API Endpoints

### Backend API

#### Контейнеры
- `GET /api/containers` - Получить список всех контейнеров (с кешем)
- `GET /api/containers/:id` - Получить информацию о конкретном контейнере
- `GET /api/containers/dependencies` - Получить граф зависимостей контейнеров
- `POST /api/containers/:id/start` - Запустить контейнер
- `POST /api/containers/:id/stop` - Остановить контейнер
- `POST /api/containers/:id/restart` - Перезапустить контейнер
- `DELETE /api/containers/:id` - Удалить контейнер (force)

#### Логи и статистика
- `GET /api/containers/:id/logs` - Получить логи контейнера (последние 100 строк)
- `GET /api/containers/:id/export-logs` - Экспорт логов в текстовый файл
- `GET /api/containers/:id/stats` - Получить статистику использования ресурсов

#### Скрипты
- `GET /api/scripts` - Получить список доступных rebuild-скриптов
- `POST /api/scripts/execute` - Выполнить скрипт для контейнера

#### Граф зависимостей
- `GET /api/graph/positions` - Получить сохраненные позиции узлов графа
- `POST /api/graph/positions` - Сохранить позиции узлов графа
- `DELETE /api/graph/positions` - Сбросить позиции узлов (reset layout)

#### Система
- `GET /api/info` - Получить информацию о Docker Engine

### WebSocket Events

#### Логи контейнеров
- `subscribe-logs` - Подписаться на логи контейнера в реальном времени
- `unsubscribe-logs` - Отписаться от логов контейнера
- `log-data` - Событие получения данных логов
- `log-error` - Событие ошибки при получении логов

#### Выполнение скриптов
- `rebuild-status-changed` - Изменение статуса rebuilding контейнера
- `script-output` - Потоковый вывод выполнения скрипта (stdout/stderr)
- `script-completed` - Завершение выполнения скрипта с exit code

## 🎨 Функционал интерфейса

### Верхняя панель (AppBar)

- **Название и версия** - отображение текущей версии приложения
- **Docker API адрес** - показывает подключенный Docker хост
- **Переключатель режимов** - Список / Плитка / Граф
- **Переключатель темы** - Светлая / Темная тема
- **Кнопка обновления** - ручное обновление данных

### Режим "Список" и "Плитка"

#### Отображение контейнеров
- Имя контейнера с индикатором rebuilding
- Docker образ
- Короткий ID контейнера
- Статус с цветовой индикацией:
  - 🟢 Running (зеленый)
  - 🔴 Exited (красный)
  - 🟡 Paused (желтый)
  - 🔵 Created (синий)
  - ⚫ Unavailable (серый, из кеша)
- Проброшенные порты
- Дата создания

#### Действия с контейнерами
- **▶️ Запуск** - зеленая кнопка (только для остановленных)
- **⏹️ Остановка** - красная кнопка (только для запущенных)
- **🔄 Перезапуск** - оранжевая кнопка (только для запущенных)
- **🗑️ Удаление** - красная кнопка с подтверждением
- **📋 Логи** - просмотр в модальном окне
- **ℹ️ Информация** - детальные метаданные
- **📊 Статистика** - графики использования ресурсов
- **🔨 Rebuild** - выполнение скрипта (если доступен)

### Режим "Граф зависимостей"

- **Интерактивный граф** на базе @xyflow/react
- **Узлы контейнеров** с цветовой индикацией статуса
- **Рёбра зависимостей** показывают связи через порты
- **Поиск узлов** - фильтрация и подсветка
- **Drag & Drop** - перемещение узлов
- **Автосохранение** позиций узлов
- **Легенда** - объяснение цветов статусов
- **Кнопка сброса** - восстановление автоматической раскладки

### Модальные окна

#### Просмотр логов
- Real-time стриминг через WebSocket
- **Цветные ANSI-коды** - сохранение bash-цветов
- Автоматическая прокрутка (можно отключить)
- Индикатор подключения
- Кнопка экспорта логов в .txt файл
- Возможность очистки
- Терминальный стиль оформления

#### Информация о контейнере
- Полное имя и ID
- Образ и версия
- Переменные окружения
- Монтированные тома
- Сетевые настройки
- Метаданные создания

#### Статистика ресурсов
- **CPU Usage** - график использования процессора
- **Memory Usage** - график использования памяти
- **Network I/O** - входящий/исходящий трафик
- **Block I/O** - операции чтения/записи на диск
- Обновление каждые 2 секунды

#### Выполнение скрипта
- Потоковый вывод stdout/stderr
- Цветной вывод с ANSI-кодами
- Индикация выполнения
- Уведомление об успехе/ошибке
- Автопрокрутка вывода

## ⚙️ Настройка

### Изменение порта Docker API

Отредактируйте `server/index.js`:

```javascript
const DOCKER_HOST = 'http://your-docker-host:2375';
```

### Изменение портов приложения

В `vite.config.js` измените порт frontend:

```javascript
server: {
  port: 3000,  // измените на нужный порт
}
```

В `server/index.js` измените порт backend:

```javascript
const PORT = process.env.PORT || 5000;  // измените на нужный порт
```

### Настройка интервала обновления

В `src/App.jsx` измените интервал автообновления:

```javascript
const interval = setInterval(fetchContainers, 5000); // 5 секунд
```

## 🔒 Безопасность

⚠️ **ВАЖНО**: Данное приложение использует незащищенное соединение с Docker API (порт 2375). 

Для использования в продакшене рекомендуется:

1. Использовать Docker API с TLS (порт 2376)
2. Настроить авторизацию в приложении
3. Использовать HTTPS для веб-интерфейса
4. Ограничить доступ к Docker API через файрвол
5. Использовать VPN для удаленного доступа

Подробнее о настройке безопасного соединения см. в файле `docker-api-setup.md`.

## 🐛 Решение проблем

### Не удается подключиться к Docker API

1. Убедитесь, что Docker API доступен:
   ```bash
   curl http://10.174.18.242:2375/version
   ```

2. Проверьте настройки файрвола на Docker хосте

3. Убедитесь, что Docker слушает на порту 2375:
   ```bash
   sudo netstat -tlnp | grep 2375
   ```

### Логи не отображаются в реальном времени

1. Проверьте консоль браузера на наличие ошибок WebSocket
2. Убедитесь, что backend сервер запущен
3. Проверьте, что контейнер генерирует логи

### Порт уже используется

Если порты 3000 или 5000 заняты, измените их в конфигурации (см. раздел "Настройка").

## 💾 Система кеширования

Приложение использует двухуровневую систему кеширования для повышения производительности и надежности:

### In-Memory кеш (node-cache)
- Хранение данных контейнеров в оперативной памяти
- Мгновенный доступ к данным (~1-5 микросекунд)
- Хранение позиций узлов графа зависимостей

### Файловая персистентность
- Автоматическое сохранение в `data/cache/containers.json`
- Сохранение позиций графа в `data/cache/node-positions.json`
- Восстановление данных при перезапуске сервера
- Отображение недоступных контейнеров при недоступности Docker API

### Автоматическое обновление
- Обновление кеша каждые 5 минут (cron)
- Мердж стратегия - сохранение истории контейнеров
- Фоновое обновление без блокировки UI

Подробнее см. [CACHE_MANAGER.md](./CACHE_MANAGER.md) и [CACHE_IMPLEMENTATION.md](./CACHE_IMPLEMENTATION.md)

## 📝 Лицензия

MIT

## 👥 Автор

Проект создан для удобного управления Docker контейнерами через веб-интерфейс.

## 🤝 Вклад в проект

Предложения и улучшения приветствуются! Создавайте Issues и Pull Requests.

## 📚 Документация

### Управление состоянием
- [ZUSTAND_STORE.md](./ZUSTAND_STORE.md) - Полное руководство по Zustand Store
- [ZUSTAND_QUICK_START.md](./ZUSTAND_QUICK_START.md) - Быстрый старт с Zustand
- [STORE_BEST_PRACTICES.md](./STORE_BEST_PRACTICES.md) - Лучшие практики Store vs useState
- [STORE_CHEATSHEET.md](./STORE_CHEATSHEET.md) - Шпаргалка по использованию Store
- [ZUSTAND_MIGRATION.md](./ZUSTAND_MIGRATION.md) - История миграции на Zustand
- [ZUSTAND_SUMMARY.md](./ZUSTAND_SUMMARY.md) - Итоги внедрения Zustand
- [ZUSTAND_VISUAL_GUIDE.md](./ZUSTAND_VISUAL_GUIDE.md) - Визуальное руководство

### Кеширование и производительность
- [CACHE_MANAGER.md](./CACHE_MANAGER.md) - Полная документация по системе кеширования
- [CACHE_IMPLEMENTATION.md](./CACHE_IMPLEMENTATION.md) - Быстрый старт с кеш-менеджером
- [REACT_COMPILER.md](./REACT_COMPILER.md) - React Compiler и оптимизация

### Архитектура и развертывание
- [ARCHITECTURE.md](./ARCHITECTURE.md) - Архитектура приложения
- [DOCKER_DEPLOYMENT.md](./DOCKER_DEPLOYMENT.md) - Развертывание в Docker
- [DOCKER_FILES_INFO.md](./DOCKER_FILES_INFO.md) - Информация о Docker файлах
- [ENV_VARIABLES.md](./ENV_VARIABLES.md) - Переменные окружения
- [PROJECT_STRUCTURE.md](./PROJECT_STRUCTURE.md) - Структура проекта

### Дизайн и UI
- [UI_DESIGN.md](./UI_DESIGN.md) - Дизайн пользовательского интерфейса
- [MOBILE_VERSION.md](./MOBILE_VERSION.md) - Адаптивная мобильная версия
- [ANSI_COLORS.md](./ANSI_COLORS.md) - Поддержка ANSI цветов в логах

## �📞 Поддержка

При возникновении проблем:
1. Проверьте раздел "Решение проблем"
2. Изучите логи в консоли браузера и терминале сервера
3. Убедитесь, что все зависимости установлены корректно
