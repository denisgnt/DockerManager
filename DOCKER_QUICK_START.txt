╔═══════════════════════════════════════════════════════════════════╗
║           DOCKER MANAGER - БЫСТРЫЙ СТАРТ В DOCKER                 ║
╚═══════════════════════════════════════════════════════════════════╝

📦 СОЗДАННЫЕ ФАЙЛЫ:
  ✅ Dockerfile              - Multi-stage образ для production
  ✅ docker-compose.yml      - Конфигурация для запуска
  ✅ .dockerignore          - Исключения для Docker сборки
  ✅ .env.docker            - Шаблон переменных окружения
  ✅ start.sh               - Скрипт быстрого запуска
  ✅ DOCKER_DEPLOYMENT.md   - Полная документация
  ✅ DOCKER_FILES_INFO.md   - Описание всех файлов

🚀 ЗАПУСК (3 СПОСОБА):

  1️⃣  АВТОМАТИЧЕСКИЙ ЗАПУСК:
      ./start.sh

  2️⃣  РУЧНОЙ ЗАПУСК:
      cp .env.docker .env
      # Отредактируйте .env под свои нужды
      docker-compose up -d

  3️⃣  С ПЕРЕСБОРКОЙ:
      docker-compose up -d --build

📝 НАСТРОЙКА .env:
  
  ОБЯЗАТЕЛЬНО настройте в .env:
  
  • VITE_DOCKER_API_HOST=10.174.18.242  # IP Docker API
  • VITE_DOCKER_API_PORT=2375            # Порт Docker API
  • SCRIPTS_DIR=/path/to/scripts         # Путь к скриптам на хосте
  
  Опционально:
  • VITE_PORT=3001                       # Порт веб-интерфейса
  • VITE_BACKEND_PORT=5001               # Порт backend API

�� ДОСТУП:
  После запуска откройте: http://localhost:3001

📊 УПРАВЛЕНИЕ:

  Просмотр статуса:     docker-compose ps
  Просмотр логов:       docker-compose logs -f
  Остановка:            docker-compose down
  Перезапуск:           docker-compose restart
  Пересборка:           docker-compose up -d --build

💾 VOLUMES:

  Директория скриптов монтируется как volume:
  • Хост: ${SCRIPTS_DIR} (из .env)
  • Контейнер: /app/scripts (read-only)
  
  Это позволяет обновлять скрипты без пересборки контейнера!

🔍 HEALTHCHECK:

  Контейнер автоматически проверяет своё здоровье каждые 30 сек.
  Проверить статус: docker inspect docker-manager --format='{{.State.Health.Status}}'

⚡ ОСОБЕННОСТИ PRODUCTION:

  ✅ Multi-stage сборка (минимальный размер образа)
  ✅ Только production зависимости
  ✅ Оптимизированный Vite bundle
  ✅ Express обслуживает статику
  ✅ SPA routing поддержка
  ✅ Healthcheck встроен
  ✅ Graceful shutdown

🛠 TROUBLESHOOTING:

  Контейнер не запускается:
    docker-compose logs docker-manager
  
  Порты заняты:
    Измените VITE_PORT в .env и пересоздайте контейнер
  
  Скрипты не работают:
    1. Проверьте SCRIPTS_DIR в .env
    2. Убедитесь, что скрипты имеют права +x
    3. Проверьте формат имен: UP_<service-name>.sh

📚 ДОКУМЕНТАЦИЯ:

  • DOCKER_DEPLOYMENT.md - Полное руководство по развертыванию
  • DOCKER_FILES_INFO.md - Описание всех Docker файлов
  • README.md - Общая информация о проекте

═══════════════════════════════════════════════════════════════════

✨ Готово! Проект упакован в Docker с поддержкой volumes для скриптов.

