# Мобильная версия Docker Manager

## Особенности мобильной версии

### 📱 Адаптивный дизайн

**Поддерживаемые размеры экранов:**
- **Mobile (XS)**: < 600px - телефоны
- **Tablet (SM)**: 600-960px - планшеты
- **Desktop (MD+)**: > 960px - десктоп

### 🎨 Адаптивные компоненты

#### AppBar
- Фиксированный с эффектом матового стекла
- Скрывает API URL на маленьких экранах (< 960px)
- Компактные кнопки переключения вида (иконки без текста на мобильных)
- Адаптивный размер заголовка

#### ContainerList

**Grid режим:**
- Mobile: 1 колонка (100% ширины)
- Tablet: 2 колонки
- Desktop: 3-4 колонки в зависимости от разрешения
- Увеличенные кнопки для touch устройств (40x40px на мобильных vs 34x34px на десктопе)
- Адаптивные отступы и шрифты

**Table режим:**
- Горизонтальный скролл на мобильных
- Все колонки доступны через прокрутку
- Сохранение настроек видимости колонок

#### Диалоговые окна

**ScriptOutput, ContainerLogs, ContainerInfo, ContainerStats:**
- Автоматический fullScreen режим на телефонах (< 600px)
- Уменьшенные отступы на мобильных
- Адаптивный размер шрифта:
  - Mobile: 0.7-0.75rem
  - Desktop: 0.875rem
- Высота графиков: 200px на мобильных, 120px на десктопе

### 📐 Touch оптимизации

**Минимальные размеры touch целей:**
- Все кнопки: минимум 44x44px (рекомендация Apple)
- Увеличенные отступы между элементами
- Flexwrap для кнопок действий

**Улучшенный скроллинг:**
- `-webkit-overflow-scrolling: touch` для плавной прокрутки на iOS
- Оптимизированные области прокрутки

**Предотвращение зума:**
- `font-size: 16px` для input элементов (предотвращает автозум на iOS)

### 🎯 Disable hover на touch устройствах
- Отключены hover эффекты на устройствах без мыши
- Карточки не поднимаются при касании

### 💾 localStorage поддержка
- Все настройки (тема, избранное, колонки) работают на мобильных
- Синхронизация между устройствами через одну учетную запись

### 🔒 PWA Ready
- Web App Capable мета-теги для iOS и Android
- Настроенный theme-color
- Translucent status bar на iOS

## Тестирование мобильной версии

### Chrome DevTools
1. Откройте DevTools (F12)
2. Toggle Device Toolbar (Ctrl+Shift+M)
3. Выберите устройство или custom размер

### Реальные устройства
Доступ через локальную сеть:
```bash
# Найдите IP вашего компьютера
ifconfig | grep inet  # macOS/Linux
ipconfig              # Windows

# Откройте на мобильном
http://<ваш-ip>:5173
```

## Горячие клавиши для тестирования

**Переключение вида:**
- Desktop: текст + иконка
- Tablet/Mobile: только иконка

**Скрытие элементов:**
- API URL: скрыт < 960px
- Текст в кнопках: скрыт < 960px

## Browser Support

✅ **Полная поддержка:**
- iOS Safari 12+
- Chrome Mobile 80+
- Firefox Mobile 80+
- Samsung Internet 12+

⚠️ **Частичная поддержка:**
- Older Android browsers (некоторые CSS эффекты могут не работать)

## Производительность

**Оптимизации:**
- Lazy loading диалогов
- Мемоизация отфильтрованных списков
- Виртуализация длинных списков (useMemo)
- Оптимизированный ререндеринг через Zustand

**Рекомендации:**
- Используйте Grid вид на мобильных (лучше производительность)
- Ограничьте количество отображаемых контейнеров через пагинацию
- Закрывайте неиспользуемые диалоги

## Известные ограничения

1. **Fullscreen API** - может не работать на некоторых устройствах
2. **Backdrop blur** - не поддерживается на старых Android устройствах
3. **Touch ripple** - может лагать на слабых устройствах

## Troubleshooting

### Проблемы с touch
Если кнопки плохо нажимаются - проверьте `mobile.css` подключен в `main.jsx`

### Зум при фокусе на input
Убедитесь что `font-size: 16px` установлен для всех input полей

### Графики не масштабируются
Recharts требует явного указания размеров - проверьте ResponsiveContainer

## Дальнейшие улучшения

- [ ] Service Worker для offline режима
- [ ] Pull-to-refresh
- [ ] Swipe жесты для карточек
- [ ] Вибрация при действиях
- [ ] Темная тема по умолчанию для OLED экранов
- [ ] Адаптивные шрифты через clamp()
