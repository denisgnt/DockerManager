# 🏗 Архитектура Docker Manager

## Общая архитектура системы

```
┌─────────────────────────────────────────────────────────────┐
│                        ПОЛЬЗОВАТЕЛЬ                          │
│                      (Веб-браузер)                           │
└──────────────────┬──────────────────────────────────────────┘
                   │
                   │ HTTP / WebSocket
                   │
┌──────────────────▼──────────────────────────────────────────┐
│                   FRONTEND (Port 3001)                       │
│  ┌────────────────────────────────────────────────────────┐ │
│  │              React 18 + Material UI                    │ │
│  │  ┌──────────────┐  ┌──────────────┐  ┌─────────────┐ │ │
│  │  │   App.jsx    │  │ Container    │  │  Container  │ │ │
│  │  │  (главный)   │  │  List.jsx    │  │  Logs.jsx   │ │ │
│  │  └──────┬───────┘  └──────┬───────┘  └──────┬──────┘ │ │
│  │         │                  │                  │        │ │
│  │         └──────────────────┴──────────────────┘        │ │
│  │                           │                            │ │
│  │         ┌─────────────────▼─────────────────┐         │ │
│  │         │  Axios (HTTP)  │  Socket.IO       │         │ │
│  │         └─────────────────┬─────────────────┘         │ │
│  └───────────────────────────┼───────────────────────────┘ │
└────────────────────────────┼─────────────────────────────┘
                             │
                   Proxy (в dev режиме)
                             │
┌────────────────────────────▼─────────────────────────────────┐
│                   BACKEND (Port 5001)                         │
│  ┌──────────────────────────────────────────────────────────┐│
│  │              Node.js + Express                           ││
│  │  ┌──────────────┐         ┌───────────────────┐         ││
│  │  │  REST API    │         │   Socket.IO       │         ││
│  │  │  Endpoints   │         │   WebSocket       │         ││
│  │  │              │         │   Server          │         ││
│  │  │ - /api/      │         │ - subscribe-logs  │         ││
│  │  │   containers │         │ - log-data        │         ││
│  │  │ - /api/info  │         │ - unsubscribe     │         ││
│  │  └──────┬───────┘         └─────────┬─────────┘         ││
│  │         │                           │                   ││
│  │         └───────────┬───────────────┘                   ││
│  │                     │                                   ││
│  │         ┌───────────▼───────────────┐                   ││
│  │         │    Axios HTTP Client      │                   ││
│  │         └───────────┬───────────────┘                   ││
│  └─────────────────────┼───────────────────────────────────┘│
└────────────────────────┼─────────────────────────────────────┘
                         │
                         │ HTTP Requests
                         │
┌────────────────────────▼─────────────────────────────────────┐
│                 DOCKER API (10.174.18.242:2375)              │
│  ┌──────────────────────────────────────────────────────────┐│
│  │              Docker Engine API v1.41+                    ││
│  │  ┌──────────────────────────────────────────────────┐   ││
│  │  │ - GET  /containers/json                          │   ││
│  │  │ - POST /containers/:id/start                     │   ││
│  │  │ - POST /containers/:id/stop                      │   ││
│  │  │ - POST /containers/:id/restart                   │   ││
│  │  │ - DELETE /containers/:id                         │   ││
│  │  │ - GET  /containers/:id/logs?follow=true          │   ││
│  │  └──────────────────────────────────────────────────┘   ││
│  └──────────────────────────────────────────────────────────┘│
└──────────────────────────────────────────────────────────────┘
```

## Поток данных

### 1. Получение списка контейнеров

```
Пользователь
    │
    │ Открывает страницу
    ▼
React App (App.jsx)
    │
    │ useEffect() -> fetchContainers()
    ▼
axios.get('/api/containers')
    │
    │ HTTP GET
    ▼
Express Backend
    │
    │ app.get('/api/containers')
    ▼
axios.get('http://10.174.18.242:2375/containers/json?all=true')
    │
    │ HTTP GET
    ▼
Docker API
    │
    │ Возвращает JSON с контейнерами
    ▼
Express Backend
    │
    │ res.json(containers)
    ▼
React App
    │
    │ setContainers(response.data)
    ▼
ContainerList.jsx
    │
    │ Отрисовывает карточки
    ▼
Пользователь видит список
```

### 2. Управление контейнером (Start/Stop/Restart)

```
Пользователь
    │
    │ Нажимает кнопку "Start"
    ▼
ContainerList.jsx
    │
    │ onAction(containerId, 'start')
    ▼
App.jsx
    │
    │ handleContainerAction()
    ▼
axios.post('/api/containers/:id/start')
    │
    │ HTTP POST
    ▼
Express Backend
    │
    │ app.post('/api/containers/:id/start')
    ▼
axios.post('http://10.174.18.242:2375/containers/:id/start')
    │
    │ HTTP POST
    ▼
Docker API
    │
    │ Запускает контейнер
    ▼
Express Backend
    │
    │ res.json({ message: 'Success' })
    ▼
React App
    │
    │ setTimeout(() => fetchContainers(), 1000)
    ▼
Обновляет список контейнеров
```

### 3. Просмотр логов в реальном времени

```
Пользователь
    │
    │ Нажимает кнопку "View Logs"
    ▼
ContainerList.jsx
    │
    │ onViewLogs(container)
    ▼
App.jsx
    │
    │ setSelectedContainer(container)
    ▼
ContainerLogs.jsx открывается
    │
    │ useEffect() -> Создает WebSocket подключение
    ▼
Socket.IO Client
    │
    │ io('http://localhost:5001')
    ▼
Socket.IO Server (Express Backend)
    │
    │ io.on('connection')
    │
    │ ◄─── socket.emit('subscribe-logs', containerId)
    │
    │ socket.on('subscribe-logs')
    ▼
axios.get('http://10.174.18.242:2375/containers/:id/logs?follow=true')
    │
    │ HTTP GET (Stream)
    ▼
Docker API
    │
    │ Стримит логи
    ▼
Express Backend
    │
    │ response.data.on('data', chunk => ...)
    │
    │ socket.emit('log-data', { containerId, data })
    │
    ▼
Socket.IO Client
    │
    │ socket.on('log-data')
    ▼
ContainerLogs.jsx
    │
    │ setLogs(prevLogs => [...prevLogs, data])
    ▼
Отображает логи в реальном времени
    │
    │ Автоскролл к последней записи
    ▼
Пользователь видит логи
```

## Компоненты и их взаимодействие

### Frontend компоненты

```
main.jsx
    │
    │ createRoot()
    │ ThemeProvider (Material UI)
    │
    └──▶ App.jsx
            │
            ├─ useState() - containers, selectedContainer, loading, error
            │
            ├─ useEffect() - fetchContainers() + auto-refresh каждые 5 сек
            │
            ├─ handleContainerAction() - start/stop/restart/remove
            │
            ├─ handleViewLogs() - открыть логи
            │
            ├──▶ ContainerList.jsx
            │       │
            │       ├─ Отображает Grid с Card для каждого контейнера
            │       │
            │       ├─ IconButton - start/stop/restart/delete/view
            │       │
            │       └─ onAction(), onViewLogs() callbacks
            │
            └──▶ ContainerLogs.jsx (если selectedContainer)
                    │
                    ├─ useState() - logs, connected
                    │
                    ├─ useRef() - socketRef, logsEndRef
                    │
                    ├─ useEffect() - создать WebSocket подключение
                    │
                    ├─ socket.emit('subscribe-logs')
                    │
                    ├─ socket.on('log-data') - добавить лог
                    │
                    ├─ Auto-scroll к последнему логу
                    │
                    └─ Dialog с логами в monospace стиле
```

### Backend маршруты

```
Express App
    │
    ├─ CORS middleware
    │
    ├─ express.json() middleware
    │
    ├─ REST API Routes:
    │   │
    │   ├─ GET    /api/containers
    │   ├─ GET    /api/containers/:id
    │   ├─ POST   /api/containers/:id/start
    │   ├─ POST   /api/containers/:id/stop
    │   ├─ POST   /api/containers/:id/restart
    │   ├─ DELETE /api/containers/:id
    │   ├─ GET    /api/containers/:id/logs
    │   ├─ GET    /api/containers/:id/stats
    │   └─ GET    /api/info
    │
    └─ Socket.IO Server:
        │
        ├─ on('connection') - новое подключение
        │
        ├─ on('subscribe-logs') - подписка на логи
        │   │
        │   ├─ axios.get(Docker API logs stream)
        │   │
        │   ├─ response.data.on('data') - получение chunk
        │   │
        │   └─ socket.emit('log-data') - отправка клиенту
        │
        ├─ on('unsubscribe-logs') - отписка
        │
        └─ on('disconnect') - закрытие соединения
```

## Безопасность и изоляция

```
┌─────────────────────────────────────────┐
│  Браузер (Frontend)                     │
│  - Не имеет прямого доступа к Docker    │
│  - Все запросы через Backend прокси     │
└──────────────┬──────────────────────────┘
               │
               │ Разрешены только:
               │ - HTTP запросы к /api/*
               │ - WebSocket к /socket.io
               │
┌──────────────▼──────────────────────────┐
│  Backend (Прокси)                       │
│  - Валидация запросов                   │
│  - Обработка ошибок                     │
│  - Логирование действий                 │
└──────────────┬──────────────────────────┘
               │
               │ Только разрешенные
               │ Docker API вызовы
               │
┌──────────────▼──────────────────────────┐
│  Docker API                             │
│  - Полный контроль над контейнерами     │
└─────────────────────────────────────────┘
```

## Масштабируемость

### Горизонтальное масштабирование

```
┌──────────┐  ┌──────────┐  ┌──────────┐
│ Client 1 │  │ Client 2 │  │ Client N │
└────┬─────┘  └────┬─────┘  └────┬─────┘
     │             │             │
     └─────────────┼─────────────┘
                   │
┌──────────────────▼─────────────────────┐
│         Load Balancer (NGINX)          │
└──────────────────┬─────────────────────┘
                   │
     ┌─────────────┼─────────────┐
     │             │             │
┌────▼────┐  ┌────▼────┐  ┌────▼────┐
│ Node 1  │  │ Node 2  │  │ Node N  │
│ (5001)  │  │ (5002)  │  │ (500N)  │
└────┬────┘  └────┬────┘  └────┬────┘
     │             │             │
     └─────────────┼─────────────┘
                   │
         ┌─────────▼─────────┐
         │    Docker API     │
         └───────────────────┘
```

### Вертикальное масштабирование

```
Увеличение ресурсов сервера:
- CPU: для обработки множества WebSocket соединений
- RAM: для кеширования данных о контейнерах
- Network: для быстрой передачи логов
```

## Технологический стек (детально)

```
┌─────────────────────────────────────────────────────────┐
│                   PRESENTATION LAYER                     │
│  ┌───────────────────────────────────────────────────┐  │
│  │ React 18.2 (UI Library)                           │  │
│  │ Material UI 5.14 (Component Library)              │  │
│  │ Emotion (CSS-in-JS)                               │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│                   APPLICATION LAYER                      │
│  ┌───────────────────────────────────────────────────┐  │
│  │ Axios (HTTP Client)                               │  │
│  │ Socket.IO Client (WebSocket)                      │  │
│  │ React Hooks (State Management)                    │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│                   NETWORK LAYER                          │
│  ┌───────────────────────────────────────────────────┐  │
│  │ Vite Dev Server (Development)                     │  │
│  │ HTTP/HTTPS (Production)                           │  │
│  │ WebSocket Protocol                                │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│                   BUSINESS LOGIC LAYER                   │
│  ┌───────────────────────────────────────────────────┐  │
│  │ Express 4.18 (Web Framework)                      │  │
│  │ Socket.IO 4.7 (WebSocket Server)                  │  │
│  │ Axios (HTTP Client to Docker API)                 │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│                   INFRASTRUCTURE LAYER                   │
│  ┌───────────────────────────────────────────────────┐  │
│  │ Node.js 18+ Runtime                               │  │
│  │ V8 JavaScript Engine                              │  │
│  │ libuv (Event Loop)                                │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│                   CONTAINER LAYER                        │
│  ┌───────────────────────────────────────────────────┐  │
│  │ Docker Engine API                                 │  │
│  │ containerd                                        │  │
│  │ runc                                              │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
```

## Развертывание

### Development

```
Terminal 1:                Terminal 2:
┌───────────────┐         ┌───────────────┐
│  npm run dev  │    →    │ Concurrently  │
└───────────────┘         └───────┬───────┘
                                  │
                    ┌─────────────┴─────────────┐
                    │                           │
            ┌───────▼────────┐         ┌───────▼────────┐
            │  npm run       │         │  npm run       │
            │  client        │         │  server        │
            └───────┬────────┘         └───────┬────────┘
                    │                           │
            ┌───────▼────────┐         ┌───────▼────────┐
            │  Vite Dev      │         │  Node.js       │
            │  Server        │         │  Express       │
            │  Port 3001     │         │  Port 5001     │
            └────────────────┘         └────────────────┘
```

### Production

```
┌─────────────────────────────────────────────────────────┐
│                     npm run build                        │
└────────────────────────┬────────────────────────────────┘
                         │
                ┌────────▼────────┐
                │  Vite Build     │
                │  (dist folder)  │
                └────────┬────────┘
                         │
┌────────────────────────▼────────────────────────────────┐
│              node server/index.js                        │
│  ┌────────────────────────────────────────────────────┐ │
│  │ Express раздает статику из dist/ + API + Socket.IO│ │
│  └────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────┘
                         │
         ┌───────────────┴───────────────┐
         │                               │
┌────────▼────────┐             ┌────────▼────────┐
│   Port 5001     │             │   Docker API    │
│  All-in-one     │             │  10.174.18.242  │
└─────────────────┘             └─────────────────┘
```

---

**Дата создания:** 2 ноября 2025 г.  
**Архитектура:** Client-Server с WebSocket  
**Паттерны:** Proxy, Observer (WebSocket), Component-based (React)
