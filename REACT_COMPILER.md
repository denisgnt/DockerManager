# React Compiler

## Обзор

React Compiler (ранее известный как React Forget) — это экспериментальный компилятор от команды React, который автоматически оптимизирует React-приложения. Компилятор понимает обычный JavaScript и правила React, что позволяет ему автоматически мемоизировать код.

## Что делает React Compiler

### Автоматическая мемоизация

React Compiler автоматически мемоизирует:
- Компоненты (вместо `React.memo`)
- Callback-функции (вместо `useCallback`)
- Вычисляемые значения (вместо `useMemo`)

### Преимущества

✅ **Улучшенная производительность** - меньше лишних ре-рендеров  
✅ **Меньше шаблонного кода** - не нужно оборачивать все в `useMemo`/`useCallback`  
✅ **Автоматическая оптимизация** - компилятор сам определяет, что нужно мемоизировать  
✅ **Совместимость** - работает с существующим кодом React

## Установка и настройка

### 1. Установка зависимости

```bash
npm install --save-dev babel-plugin-react-compiler --legacy-peer-deps
```

### 2. Конфигурация Vite

В файле `vite.config.js`:

```javascript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

const ReactCompilerConfig = {
  // Опциональные настройки
}

export default defineConfig({
  plugins: [
    react({
      babel: {
        plugins: [
          ['babel-plugin-react-compiler', ReactCompilerConfig],
        ],
      },
    }),
  ],
})
```

## Опции конфигурации

### target

Целевая версия React для компиляции:

```javascript
const ReactCompilerConfig = {
  target: '19', // '17' | '18' | '19'
}
```

### compilationMode

Режим компиляции компонентов:

```javascript
const ReactCompilerConfig = {
  compilationMode: 'infer', // 'infer' | 'annotation'
}
```

- **`infer`** (по умолчанию) - автоматически компилирует все компоненты
- **`annotation`** - компилирует только компоненты с директивой `'use memo'`

Пример использования режима `annotation`:

```javascript
function MyComponent() {
  'use memo' // Директива для компиляции
  // ... код компонента
}
```

### sources

Функция фильтрации файлов для компиляции:

```javascript
const ReactCompilerConfig = {
  sources: (filename) => {
    // Компилировать только файлы из папки src
    return filename.includes('/src/')
  }
}
```

## Использование

### До React Compiler

```javascript
import { memo, useMemo, useCallback } from 'react'

const ExpensiveComponent = memo(({ data, onUpdate }) => {
  const processedData = useMemo(() => {
    return data.map(item => item * 2)
  }, [data])

  const handleClick = useCallback(() => {
    onUpdate(processedData)
  }, [processedData, onUpdate])

  return <div onClick={handleClick}>...</div>
})
```

### После React Compiler

```javascript
// React Compiler автоматически оптимизирует!
function ExpensiveComponent({ data, onUpdate }) {
  const processedData = data.map(item => item * 2)

  const handleClick = () => {
    onUpdate(processedData)
  }

  return <div onClick={handleClick}>...</div>
}
```

## Проверка работы компилятора

### 1. Режим разработки

```bash
npm run dev
```

Компилятор работает в режиме разработки и в консоли браузера можно увидеть предупреждения, если что-то не оптимизировано.

### 2. Продакшн сборка

```bash
npm run build
```

### 3. Проверка в React DevTools

В React DevTools можно увидеть, какие компоненты были оптимизированы компилятором.

## Ограничения и правила

### React Compiler работает лучше всего, когда:

✅ Код следует [правилам React](https://react.dev/reference/rules)  
✅ Компоненты являются чистыми функциями  
✅ Используются правильные зависимости в хуках

### Потенциальные проблемы:

⚠️ **Мутация объектов** - компилятор предполагает, что данные иммутабельны  
⚠️ **Побочные эффекты** - побочные эффекты должны быть в `useEffect`  
⚠️ **Ref-ы** - осторожность при работе с ref-ами

## Миграция существующего кода

### Постепенная миграция

1. Установите компилятор
2. Начните с режима `annotation`:
   ```javascript
   const ReactCompilerConfig = {
     compilationMode: 'annotation'
   }
   ```
3. Добавляйте `'use memo'` в компоненты постепенно
4. Тестируйте каждый компонент
5. Когда всё протестировано, переключитесь на режим `infer`

### Удаление ненужной мемоизации

После включения компилятора можно постепенно удалять:
- `React.memo` обёртки
- `useMemo` для простых вычислений
- `useCallback` для обработчиков событий

**Примечание**: Оставьте `useMemo`/`useCallback` для действительно дорогих операций или когда они нужны для семантики (например, в зависимостях `useEffect`).

## Отладка

### Просмотр скомпилированного кода

Чтобы увидеть, как компилятор преобразовал код, можно использовать [React Compiler Playground](https://playground.react.dev/).

### Отключение компилятора для конкретного файла

Если компилятор вызывает проблемы в конкретном файле, можно отключить его:

```javascript
// Вверху файла
/* @babel-plugin-react-compiler-ignore */

function ProblematicComponent() {
  // ... код
}
```

### Директива opt-out для компонента

```javascript
function MyComponent() {
  'use no memo' // Отключить мемоизацию для этого компонента
  // ... код
}
```

## Производительность

### Измерение эффекта

1. **До компилятора**: используйте React DevTools Profiler
2. **После компилятора**: сравните количество ре-рендеров

### Ожидаемые улучшения

- 🚀 Меньше ре-рендеров при изменении пропсов
- 🚀 Лучшая производительность списков
- 🚀 Оптимизация контекстов
- 🚀 Меньше размер бандла (при удалении ручной мемоизации)

## Совместимость

- ✅ React 17+
- ✅ React 18
- ✅ React 19 (рекомендуется)
- ✅ TypeScript
- ✅ JavaScript
- ✅ Vite
- ✅ Webpack
- ✅ Next.js

## Полезные ссылки

- [Официальная документация React Compiler](https://react.dev/learn/react-compiler)
- [React Compiler Playground](https://playground.react.dev/)
- [Babel Plugin React Compiler](https://www.npmjs.com/package/babel-plugin-react-compiler)
- [Правила React](https://react.dev/reference/rules)

## Статус проекта

React Compiler находится в активной разработке и используется в production в Meta (Facebook, Instagram). Для новых проектов на React 19 рекомендуется к использованию.

---

**Последнее обновление**: 6 ноября 2025 г.  
**Версия компилятора**: 1.0.0  
**Версия React**: 19.2.0
