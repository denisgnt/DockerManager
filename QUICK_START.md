# Docker Manager - Краткая справка

## 🚀 Быстрый старт

```bash
# 1. Установка зависимостей
npm install

# 2. Создание конфигурации
cp .env.example .env

# 3. Настройка .env (при необходимости)
# Откройте .env и укажите свои настройки Docker API

# 4. Запуск в режиме разработки
npm run dev
```

После запуска:
- Frontend: http://localhost:3001 (настраивается через VITE_PORT в .env)
- Backend: http://localhost:5001 (настраивается через VITE_BACKEND_PORT в .env)
- Docker API: адрес отображается в верхней панели (из VITE_DOCKER_API_HOST и VITE_DOCKER_API_PORT)

## 📱 Основные функции

### 1. Просмотр контейнеров
- Все контейнеры отображаются в виде карточек
- Цветные индикаторы статуса:
  - 🟢 Зеленый - контейнер запущен (running)
  - 🔴 Красный - контейнер остановлен (exited)
  - 🟡 Желтый - контейнер на паузе (paused)

### 2. Управление контейнерами

**Кнопки управления:**
- ▶️ **Play** - Запустить контейнер
- ⏹️ **Stop** - Остановить контейнер
- 🔄 **Restart** - Перезапустить контейнер
- 🗑️ **Delete** - Удалить контейнер (с подтверждением)
- 👁️ **View** - Просмотреть логи контейнера

### 3. Просмотр логов

- Открывается в модальном окне
- Логи обновляются в реальном времени через WebSocket
- Автоматическая прокрутка к последним записям
- Кнопка "Очистить" для очистки отображаемых логов
- Индикатор подключения (зеленый/красный)

## 🔧 Архитектура

```
┌─────────────────┐
│   React UI      │  Port 3001
│  (Material UI)  │
└────────┬────────┘
         │
         │ HTTP/WebSocket
         │
┌────────▼────────┐
│  Express Server │  Port 5001
│  + Socket.IO    │
└────────┬────────┘
         │
         │ HTTP
         │
┌────────▼────────┐
│   Docker API    │  10.174.18.242:2375
└─────────────────┘
```

## 🎨 Используемые технологии

### Frontend
- **React 18** - UI библиотека
- **Material UI 5** - компоненты дизайна
- **Socket.IO Client** - WebSocket для real-time логов
- **Axios** - HTTP клиент
- **Vite** - сборщик и dev сервер

### Backend
- **Express** - веб-фреймворк
- **Socket.IO** - WebSocket сервер
- **Axios** - проксирование запросов к Docker API

## 🔍 API Endpoints

| Метод | Путь | Описание |
|-------|------|----------|
| GET | `/api/containers` | Список всех контейнеров |
| GET | `/api/containers/:id` | Детали контейнера |
| POST | `/api/containers/:id/start` | Запустить контейнер |
| POST | `/api/containers/:id/stop` | Остановить контейнер |
| POST | `/api/containers/:id/restart` | Перезапустить контейнер |
| DELETE | `/api/containers/:id` | Удалить контейнер |
| GET | `/api/containers/:id/logs` | Получить логи |
| GET | `/api/containers/:id/stats` | Статистика контейнера |

## 🐛 Часто встречающиеся проблемы

### Ошибка подключения к Docker API

**Проблема:** "Ошибка подключения к Docker API"

**Решение:**
```bash
# Проверьте доступность Docker API
curl http://10.174.18.242:2375/version

# Если не работает, проверьте настройки Docker на удаленном хосте
# См. файл docker-api-setup.md
```

### Порты уже заняты

**Проблема:** "Port 3000/5000 is already in use"

**Решение:**
- Vite автоматически выберет другой порт (3001, 3002 и т.д.)
- Или измените порт вручную в `vite.config.js` и `server/index.js`

### Логи не обновляются

**Проблема:** Логи контейнера не отображаются в реальном времени

**Решение:**
1. Проверьте консоль браузера (F12) на ошибки WebSocket
2. Убедитесь, что backend сервер запущен
3. Проверьте, что контейнер генерирует логи

## 💡 Советы по использованию

1. **Автообновление** - список контейнеров обновляется автоматически каждые 5 секунд
2. **Ручное обновление** - нажмите кнопку 🔄 в правом верхнем углу
3. **Множественные действия** - можете управлять несколькими контейнерами одновременно
4. **Логи** - откройте логи нескольких контейнеров в разных вкладках браузера

## 🔐 Безопасность

⚠️ **ВНИМАНИЕ**: Данная версия использует незащищенное подключение к Docker API.

Для продакшена необходимо:
- Использовать TLS (порт 2376)
- Добавить авторизацию
- Настроить HTTPS
- Ограничить доступ файрволом

## 📚 Дополнительные ресурсы

- [Docker API Reference](https://docs.docker.com/engine/api/latest/)
- [Material UI Documentation](https://mui.com/)
- [Socket.IO Documentation](https://socket.io/docs/)
- [React Documentation](https://react.dev/)

## 🛠 Разработка

### Структура компонентов

```
src/
├── App.jsx                 # Главный компонент приложения
├── main.jsx                # Точка входа React
└── components/
    ├── ContainerList.jsx   # Список контейнеров (карточки)
    └── ContainerLogs.jsx   # Модальное окно с логами
```

### Добавление новых функций

1. **Добавить endpoint в backend** (`server/index.js`)
2. **Создать функцию в App.jsx** для вызова API
3. **Обновить UI компоненты** для отображения данных

### Запуск в production

```bash
# Создать production сборку
npm run build

# Запустить production сервер
node server/index.js

# Раздавать статику из папки dist
```

---

**Версия:** 1.0.0  
**Дата:** 2 ноября 2025 г.
